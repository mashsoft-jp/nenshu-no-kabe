# 開発ルール — 年収の壁

## 作業開始

`PROJECT.md` → `docs/SPEC.md` → `docs/DECISIONS.md` → `docs/BACKLOG.md` を読む。基準版はv0.7。変更依頼と最新の承認事項を優先し、現行実装の制約を恒久要件へ格上げしない。

仕様書、説明、作業報告は日本語。コードの識別子は既存の形式を維持する。仕様上の呼称は「年収の壁」。機能と操作内容を直接説明する文言にし、広告的な表現を追加しない。

## 維持する要件

- 月給×12＋任意の賞与を年収とし、一つの入力・ページで通常月と年間を表示。
- 内訳と計算根拠を分離せず、各項目の「詳細」で展開。開いた状態での再計算、複数展開、キーボード操作を維持。
- 所得税変更は手取り・内訳より下の折りたたみオプション。現行側は固定し、仮想側のみ変更。
- 入力値の外部送信、自動保存、アクセス解析、認証、DB、外部フォントなどを無断追加しない。

## ソースと計算

`index.html`は生成物・Git管理対象外。直接編集せず、分離されたJS/CSS/HTMLとビルド処理を変更する。計算部にDOMや通信を持ち込まない。円・率の単位と丸め順を明示し、月給分と賞与分を区別する。

通常月の源泉徴収と年間所得税、当年度の給与天引き住民税と翌年度負担の概算を混ぜない。年間手取りを通常月手取りの12倍へ置き換えない。

現行の税率や控除を変更する場合、`SOURCES.md`の記述だけを根拠とせず一次資料を再確認する。公表日・成立/施行・所得年・課税年度・対象月を区別する。境界値・端数・上限について独立した期待値を追加し、ソースとテストが同じ誤りを共有していないか確認する。

旧JSONには互換識別子`tedori-policy`が残る。アプリ名変更だけを理由に形式名を壊さない。形式変更時はバージョン・移行・不正入力テストを追加する。

## 検証と報告

```sh
python3 build.py
node test-monthly.cjs
node test-policy.cjs
node test-unified.cjs
```

UI変更時は利用可能なブラウザで `python3 test-unified-ui.py` を実行し、少なくとも狭幅の表示、詳細展開、オプション、JSON/CSV、入力エラーを確認する。未実行の検証は未実行と記録する。

UI改修と制度改定は別の変更として扱う。受入条件・変更した仕様・テスト結果・残課題を記録する。全面リライトやフレームワーク移行は、現行維持との差を提示し、承認後に行う。

## 外部操作・秘密情報

対象はprivateの `mashsoft-jp/nenshu-no-kabe`。mainへのマージ、公開範囲変更、デプロイ、課金、別リポジトリの操作は依頼なしに行わない。ライセンス・公開先は未決定。既存環境の変更は最小限とし、認証情報や利用者の給与設定をコミットしない。検証は合成データで行う。

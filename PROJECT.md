# 年収の壁 — プロジェクト概要

整理日：2026-09-09 / 基準版：v0.7。承認済み要件と現行実装の暫定条件を区別する。

## 目的

月給から税金・社会保険料を差し引いた手取りを視覚的に把握するWebアプリ。月給と年間を一つのページで扱い、`月給 × 12 ＋ 任意の賞与` を年収とする。所得税の区分境界・率・段階数を変更して現行制度との手取り差も比較する。

日本の現時点の制度に基づくことがユーザー要求。健康保険はまず協会けんぽ。現在の試作コードが法令へ完全準拠していると保証せず、一次資料の再確認と独立検算を公開前の課題にする。

## ユーザー確定事項

| 項目 | 内容 |
| --- | --- |
| 名称 | 年収の壁。テレビ番組「有吉の壁」をもじった名称。2026-09-10にユーザーが確認した透過画像をロゴに採用（docs/DECISIONS.md参照） |
| 入力・表示 | 月給×12＋任意賞与、通常月と年間を一つのページで見る |
| 内訳 | 各項目の「詳細」でその場に式・適用条件を展開 |
| 優先順位 | 手取り表示が主、所得税変更はその下のオプション |
| 文言 | キャッチコピー調を避け、内容を直接示す簡潔な表現 |
| 開発 | ローカルZIP受渡しではなくGitHubで継続開発 |
| 保存先 | `mashsoft-jp/nenshu-no-kabe`。ユーザー作成。privateを接続時に確認 |

## 実装と管理

HTML/CSS/JavaScriptの分離ソースを `build.py` で単体の `index.html` にする。生成物はGit管理対象外。初期登録ではアプリソースをv0.7から変更しない。React / TypeScript / Next.js等への移行はまだ決定していない。

基本入力は月給、賞与の月と額面、加入支部、年齢。詳細条件では標準報酬月額、非課税通勤、雇用保険、住民税、年間保険料、その他控除を指定できる。通常月と年間の手取り、年額÷12の月平均、月額・年額の内訳、年収別グラフを表示。JSON保存・読込、CSV、コピーあり。

入力値をサーバーへ送信せず、自動保存・解析はない。ページの再読込で入力は失われ、明示的なJSON保存で残す。

初期登録は `chore/import-v0.7-project` からmainへのPRで確認する。mainに直接上書き・自動マージしない。クラウド実行環境、CI、GitHub Issues化、公開サイトはこの初期登録とは別作業。アプリの公開設定や他のリポジトリは変更しない。

## 現行の暫定条件・未対応

20〜64歳、国内居住、協会けんぽ・厚生年金加入済み、配偶者・扶養・追加控除入力、給与所得のみ、毎月同額。賞与は最大3件。全国自治体の住民税自動計算は未対応で、東京23区の限定概算または通知書の直接入力を使う。

社会保険加入・脱退や被扶養者認定、世帯手取り、控除対象の支払額からの自動算定、年4回以上の賞与、賞与ごとの源泉徴収・振込額、年途中の資格・年齢・料率変更等は未対応。これらは試作制限であり、最終製品の確定要件ではない。

## 通常月と年間の違い

現行コードは通常月に2026年9月の源泉徴収と2026年度住民税、年間に2026年分所得税・2027年度住民税と2026年9月の保険料率による年額モデルを使う。これは既存コードの説明で、今回の移行で最新法令を確認したという意味ではない。

年間表示は実際の2026年1〜12月の入金合計ではない。名称だけからあらゆる扶養・社会保険の壁に対応すると案内しない。

## 次の作業

`docs/BACKLOG.md` のCALC-001から一次資料・適用時期・端数・独立期待値を整理する。制度改定・UI変更・モデル拡張を別々の課題にする。ライセンス、一般公開、ホスティング、運営方針、フレームワークは未決定。

仕様・ソース・決定履歴はGitHub側を正本として更新する。会話の内容がリポジトリへ自動同期される前提にはせず、変更依頼に対応する仕様・テストも明示して更新する。

扶養・追加控除の仕様、一次資料、検証は `docs/DEPENDENTS.md` と `docs/DEDUCTIONS.md` を参照。2026-09-10に配偶者・その他控除の追加と、検証・Bugbot確認後のマージを承認済み。

2026-09-10追記：結果の上端固定サマリー、給与条件・内訳項目の初期折りたたみ、基礎控除・給与所得控除スライダーを追加。給与所得控除は仮想側だけ全所得帯固定にできる。JSON version 8。詳細はdocs/SPEC.md末尾を参照。

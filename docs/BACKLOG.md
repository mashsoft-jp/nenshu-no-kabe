# 開発課題・Issue候補

この一覧は提案であり、GitHubにIssueを作成した記録ではない。優先順位と対応範囲は今後の承認対象。P0は一般公開前の確認候補、P1は継続開発の基盤、P2は機能拡張候補。

## 実施済みの準備

- [x] v0.7のソースとビルド手順を保全。
- [x] 会話上の確定要件、現行実装の制約、未決事項を整理。
- [x] ビルド再現性、既存エンジンテスト、既存ブラウザテストを再実行。
- [x] 開発手順、ChatGPT指示文、AIへの引き継ぎ文を用意。
- [x] ユーザー作成のprivateリポジトリ `mashsoft-jp/nenshu-no-kabe` へのアクセスを確認。

## P0 — 計算と利用範囲

### CALC-001 一次資料と実装の照合
状態：未着手。`SOURCES.md`を出発点として国税庁・協会けんぽ・日本年金機構・厚生労働省・対象自治体の最新一次資料を確認する。

完了条件：項目ごとに適用年/年度/対象月、成立・施行・反映時期、確認日、出典、実装関数、端数処理、独立した期待値を対応付ける。法令上の金額と比較用の仮定を分け、不一致を課題登録する。既存テストの通過だけで完了としない。

### MODEL-001 年間表示の意味とラベル
状態：未着手。現状の年額負担モデルを維持するか、実際の1〜12月の収支も扱うかを決める。

完了条件：通常月、年間負担、月平均、賞与の個別振込額を明確に区別した仕様を承認する。現状のモデルを実際の年間振込額と表示しない。年度をまたぐ住民税・賞与枠、年末調整の扱いを明記する。

### SCOPE-001 「年収の壁」の対応範囲を明示
状態：未着手。名称から社会保険や扶養の全ての壁に対応すると誤解されない表示を確認する。

完了条件：所得税区分の比較と社会保険加入・被扶養者認定が別機能であること、現行の加入済み・扶養なしモデルの制約が画面から理解できる。未対応を無条件に0円計算しない。

### QA-001 金額と境界の独立テスト
状態：未着手。既存回帰テストを保ち、一次資料または別経路の手計算に基づくケースを追加する。

完了条件：区分境界の前後1円、等級境界、端数、賞与の同月合算・年度/月上限、前年枠、非課税通勤、年額上書き、JSON移行を確認する。期待値の根拠と対象期間を保存し、実装をコピーして期待値を作らない。

## P1 — 継続開発の基盤

### DATA-001 制度データの分離
状態：提案。料率・控除・等級と計算ロジックを段階的に分離する。

完了条件：対象年、施行/適用期間、出典、確認状態を持つデータ構造を定義する。現行結果との比較で差を説明できる。未確認の翌年データへ自動で切り替えない。フレームワーク変更を前提にしない。

### DEV-001 Git / GitHubの管理開始
状態：privateリポジトリはユーザーが作成済み。本変更がv0.7の初期登録。mainへのマージ、GitHub Issues化、CI設定は別作業。

完了条件：指定された所有者配下・公開範囲で初期コミットを確認し、必要なCIと課題管理を整備する。機密ファイルを含めない。クラウド実行の手順と費用・公開範囲を確認し、未承認のデプロイや課金を行わない。

### QA-002 配布形態・ブラウザ・アクセシビリティ
状態：追加検証待ち。既存テストはChromiumにHTML文字列を読み込む方式。

完了条件：実際のfile://起動とHTTP配信、Safari/Firefox/Chromium、狭幅実機で操作・保存・コピーを確認する。フォーカス、項目詳細の展開、エラー通知、キーボード操作、縮小表示の不具合を解消する。外部通信も意図したものだけか検査する。

### RELEASE-001 公開前の仕様・案内・権利確認
状態：公開方針未決定。

完了条件：公開する計算範囲、運営・問い合わせ、データ取扱い、概算表示、依存ライセンス、名称・素材の扱い、公開先、ロールバック方法を決める。法律上必要な事項は個別に確認する。番組との公式な関係を示唆しない。

## P2 — 機能拡張候補（実装の承認はまだない）

| ID | 候補 | 着手前に決めること |
| --- | --- | --- |
| EXT-001 | 社会保険の加入・扶養に関する壁 | 勤務条件、世帯条件、事業所条件、対象制度・時期 |
| EXT-002 | 配偶者・扶養・追加控除 | 配偶者以外の扶養区分別入力をローカル実装・検証済み（docs/DEPENDENTS.md）。配偶者・その他控除は未対応 |
| EXT-003 | 全国自治体の住民税 | 地域差・非課税要件のデータと更新体制 |
| EXT-004 | 賞与ごとの源泉徴収・振込額 | 支給前月の条件、支給月、控除、適用時期 |
| EXT-005 | 実際の暦年の入出金 | 月別給与・資格・料率、住民税通知、年末調整 |

## 最初の作業案

CALC-001の調査表を作り、実装と一次資料の差を挙げる。差を見つけても一括でコードを書き換えず、表示改修・制度更新・モデル拡張を別々の課題に分ける。

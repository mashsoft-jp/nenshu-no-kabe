# 決定事項

2026-09-09整理。会話順の記録であり、各発言の正確な日時を推測して付けない。

| ID | 内容 | 状態・根拠 |
| --- | --- | --- |
| D01 | 日本の現時点の制度に基づく給与の手取り可視化Webアプリ | ユーザー要求 |
| D02 | 健康保険はまず協会けんぽ | ユーザー指定 |
| D03 | 所得税の区分境界・率・段階数を変更して手取り差を見る | ユーザー要求 |
| D04 | 所得税変更は主機能ではなく、手取りより下のオプション | ユーザー指定 |
| D05 | キャッチコピーを避け「内訳」等の簡潔な項目名 | ユーザー指定 |
| D06 | 名称「年収の壁」。番組「有吉の壁」をもじる | ユーザー指定。番組素材の利用は要件にない |
| D07 | 内訳と計算根拠を統合。各項目の「詳細」で式を表示 | ユーザー要求 |
| D08 | 月給と年間を別ページにせず、月給×12＋賞与を年収とする | ユーザー要求 |
| D09 | 継続開発できるプロジェクトにまとめる | ユーザー要求。初回整理時点では外部サービス作成先未指定 |
| D10 | ローカルのZIP受渡しではなくGitHubで継続開発 | ユーザー要求 |
| D11 | 保存先は `mashsoft-jp/nenshu-no-kabe` | ユーザーが作成しURLを指定。privateを接続時に確認 |
| D12 | v0.7を作業ブランチに初期登録し、mainへの確認用PRを作成 | 初期移行の方針。マージと公開は別承認。後続D13・D14を参照 |
| D13 | Bugbotの正常終了・指摘なしを確認し、初期PR #1をmainにマージ | ユーザーの「はい」で承認。マージコミット `37ed609665c70e587bd233a2867edbb78eda780c`。以後mainを開発の基点にする |
| D14 | 公開先をGitHub Pagesにする | ユーザー依頼「GitHub Pagesにしてほしい」。リポジトリはprivateを維持。設定PRとPages有効化・デプロイ成功を区別する |

## 実装済みだが最終要件ではない条件

賞与最大3件、20〜64歳・扶養なし、東京23区の住民税概算、単体HTML、ブラウザ内計算、基礎控除編集、設定JSON・CSV、年額モデルの適用年度はv0.7の実装条件である。今回変更せず引き継ぐが、ユーザーが最終製品の仕様として個別承認したとは扱わない。

## 初期移行の管理方法

ソース・テスト・仕様を個別ファイルで登録する。生成物 `index.html` は `python3 build.py` で再現し、重複するソースとしてはコミットしない。生成後の内容は元のv0.7と照合する。この管理上の変更でアプリの画面や計算は変更しない。

初期コミット前のmainはREADMEのみだった。初期PR #1はD13でマージ済み。

## Pages公開の構成

D14に対する実装は `.github/workflows/pages.yml`。main向けPRでテスト・ビルド・公開ファイルの検査を行い、mainへのpushまたはmainの手動実行時だけ公開する。生成HTML1ファイルのみを一時ディレクトリからアップロードする。開発資料・テスト・Git履歴・利用者設定は配信対象外。HTML内のクライアントコードは閲覧できる。

実際の公開にはGitHub側のPages有効化と対応プランが必要。リポジトリの公開化、契約アップグレード、予算の変更は承認に含めず、必要ならユーザーへ確認する。CI・Bugbotの確認前の自動マージは設定しない。通常のPagesサイトは非公開プレビューとは異なる。

Pages対応はホスティング設定であり、CALC-001 / QA-001等の税制・計算の独立検証を完了させたものではない。既存資料の「公開先未決定」「初期PR未マージ」は初期登録時点の記録で、現在の状態はD13・D14と `docs/PAGES.md` を優先する。

## 未決定

ライセンス、独自ドメイン、収益化、認証・DB、フレームワーク、将来対応する扶養・社会保険加入・自治体の範囲は未決定。Codespaces等のクラウド開発環境の作成、課題のGitHub Issues化、計算の独立検証も別途行う。Pagesの初回公開完了は実URLで確認してから報告する。

## 更新方法

決定ごとに「内容・決定者/依頼・状態・理由・影響」を追加する。撤回や変更は履歴を消さず、新しい記録から旧記録を参照する。仕様と課題も同時に更新する。

## 扶養条件の追加（2026-09-09）

ユーザーの「扶養条件を入力できるようにして」に対応。配偶者以外の国内居住親族について区分別人数入力を追加し、通常月の源泉徴収・年間所得税・住民税概算へ反映する。月額と年額、今年と前年の条件を区別する。詳細範囲と対象外は `docs/DEPENDENTS.md`。既存の扶養なしは初期値として維持し、恒久的な制限にはしない。本変更のmainへのマージ・push・公開はまだ依頼されていない。

2026-09-10追記：ユーザーの「マージして公開して」により、この扶養条件の変更についてPR作成・push・mainへのマージ・既存GitHub Pagesへの公開を承認。CI・Bugbotの確認後に実施し、公開結果はGitHubの実行と実URLで確認する。


## 配偶者・その他の控除へ拡張（2026-09-10）

PR #5のマージ直前にユーザーから配偶者控除・他の控除の追加依頼を受け、マージを保留。資格条件から判定する人的控除と、算定済み金額を入力する保険・医療費等の所得控除、住宅ローン等の税額控除を区別して追加する。対象範囲・前提・一次資料は `docs/DEDUCTIONS.md`。

ユーザーが「完了したら push → PR 作成 → bugbot からの指摘無ければマージ」と明示的に承認。未マージのPR #5を今回の最終仕様に更新し、追加変更を含む最新コミットでCI・Bugbotを確認してからマージする。既存Pagesの自動配信も含め、実際の完了状態を確認して報告する。

## 2026-09-10 子ども・子育て支援金の端数処理

利用者の依頼により、当面、支援金の本人負担額は標準報酬月額・標準賞与額 × 0.23% ÷ 2 を単独計算し、1円未満を切り上げる。月給分は月額を切り上げてから12倍し、賞与分は既存の同月合算・年度上限適用後に切り上げる。健康保険等の端数処理は維持する。法定の一律切上げへの制度改定ではなく、給与明細に合わせる計算上の設定として詳細に明記する。

検証：ビルド、test-monthly.cjs、test-policy.cjs、test-unified.cjs（既存の住民税・扶養・追加控除テストを含む）成功。支援金の小数・50銭・整数・ゼロ、月給と賞与の年額、同月賞与合算、年度上限の独立期待値18件を追加。test-unified-ui.py は5画面幅の135チェックと追加アサーション成功（詳細展開、オプション、JSON/CSV、入力エラー）。公開サイトへの反映は未実施。

## 2026-09-10 月給から住民税を天引きしない選択

「通常月の住民税」に「月給から天引きしない（自分で別途支払う）」を追加。通常月の住民税は0円、年間の税額・年間手取りは従来どおり別払いの負担を含める。年額・年額計算方法を変更しても天引きなしを維持する。別払い時は年額入力の説明を切り替え、給与特別徴収の配分と6月手取りは非表示、CSVの配分欄は空欄とする。普通徴収の各期納付額は計算しない。

JSON version 6に `monthlyResidentMode: none` を追加。旧version 1〜5は既存の移行を維持し、天引き方法を勝手に変えない。旧versionでのnone指定と未知のモードは拒否する。既定は従来どおり前年同額の概算。受入条件は月額のみ0円・年額維持、年額編集後の選択維持、JSON復元、狭幅での表示。

検証結果：ビルド、月次・制度・統合計算テスト（住民税、扶養、追加控除を含む）成功。天引きなしと年額独立性・旧JSON維持・不正モード拒否を追加検証。画面はtest-unified-ui.pyの135チェック、test-resident-notice-ui.pyの55チェックと追加アサーションが5画面幅ですべて成功。公開反映は未実施。

## 2026-09-10 初期設定のステップ入力

初期表示を「給与・賞与」「社会保険」「扶養・控除」「住民税」「入力内容の確認」の5ステップにする。設定中は結果・グラフ・所得税変更を隠し、「手取りを表示」で既存の同一ページの通常月・年間表示に進む。計算・入力の初期値は変更しない。

戻る操作は入力値を保持する。扶養・追加控除は必要な項目だけ展開。「一覧で入力する」で従来の編集画面へ切り替え、給与条件の「ステップ式で入力」で再開できる。既存の入力要素を移動して利用し、状態を複製しない。保存・読込は既存JSON version 6を維持。進行状況の自動保存や送信は追加しない。

各ステップの「次へ」で現在の欄の形式・範囲を検証し、不正な入力があればその場で案内する。他のステップの未入力・不正値で移動を止めず、最終表示前に全体の計算条件を検証する。戻る・一覧への切り替えは修正のために常時利用できる。ステップ移動後は見出しへフォーカスし、入力欄のEnterでも次へ進める。最終確認で主要条件と既定値利用の注意を表示する。

受入条件：初期状態では給与入力のみ表示、前後移動で値保持、住民税別払い・扶養入力、最終結果の一致、一覧との往復、設定復元、エラー停止、320〜1440pxで横はみ出しなし。制度変更とは別のUI変更として実装。

検証：ビルドと既存の月次・制度・統合計算テスト成功。test-setup-wizard.pyは5画面幅で前後移動、入力エラー、Enter操作、フォーカス、最終結果、一覧復帰、再開、JSON読込と非送信を確認。390pxの確認画面画像を目視確認。既存UIも5幅で統合135・住民税55・扶養45・追加控除45・税率グラフ746チェックと追加アサーション成功。公開サイトへの反映は未実施。

PR #6 Bugbot対応：後続ステップに不正値を残して戻った場合も、そのステップへ再び進んで修正できるようにした。現在の欄の不正入力は停止し、全体の整合性は結果表示前に確認する。空欄の年齢を残して前へ戻り、再度進んで修正する回帰テストを追加。

## 2026-09-10 報酬の区分と雇用保険

最初の入力に「従業員の給与」「代表取締役・役員報酬のみ」「兼務役員・その他（個別指定）」を追加。従業員は一般事業0.5%を既定とし、既に0.6%を選んでいれば保持する。役員報酬のみへ切り替えると月給・賞与とも雇用保険0、保険料率欄は編集不可。他の社会保険や税率は変更しない。従業員から非加入を直接選んだ場合は個別指定に切り替える。

代表取締役・業務執行権のある取締役は被保険者とならず、その他役員も原則対象外。雇用関係が認められる兼務役員は例外があるため個別指定を残す。ただし役員報酬と雇用保険対象の給与が混在する計算は未対応と明示し、一律に全役員を非加入と断定しない。一次資料確認：北海道ハローワーク「雇用保険の被保険者の種類と要件」 https://jsite.mhlw.go.jp/hokkaido-hellowork/list/sapporo/jigyosyomainhokennryoumada/hoken3/01.html （2026-09-10閲覧）。保険料率改定ではなく入力連動の追加。

JSON version 7にpayType（employee/officer/custom）を保存。旧version 1〜6は雇用保険率を変更せず、0ならcustom、それ以外ならemployeeとして移行する。旧ファイルから役員と推測しない。version 7の区分欠落・未知値、officerと非ゼロ保険料率の矛盾は拒否する。確認画面・CSV・コピーにも報酬区分を含める。給与設定の自動送信・自動保存は追加しない。

検証結果：ビルド、既存月次・制度・統合計算テスト成功。役員の月給・賞与の保険料0、矛盾設定の拒否、旧JSONの料率維持を追加検証。test-pay-type-ui.py、test-setup-wizard.py、test-unified-ui.pyを5画面幅で実行し成功（切替、非活性、保存読込、旧形式移行、入力エラー、詳細、JSON/CSVを含む）。公開サイトへの反映は未実施。
